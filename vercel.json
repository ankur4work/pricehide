{
  "buildCommand": "echo DATABASE_URL=$DATABASE_URL | sed 's/:.*@/:***@/' && npx prisma generate && npx prisma migrate deploy && npx remix vite:build",
  "installCommand": "npm install --include=dev",
  "framework": "remix"
}
